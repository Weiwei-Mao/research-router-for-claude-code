{
  "session_dir": "data/sessions",
  "default_analyzer": "qwen",
  "providers": {
    "codex": {
      "command": ["codex", "exec"],
      "timeout": 600
    },
    "qwen": {
      "command": ["qwen"],
      "timeout": 600
    },
    "gemini": {
      "command": ["gemini", "-p", ""],
      "timeout": 600
    }
  }
}
